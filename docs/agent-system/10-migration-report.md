# Migration report

Governance moves from small `.agents` notes and shell print hooks to native `.claude` instructions, rules, skills, specialist agents and enforceable hooks. Legacy `.agents` paths remain as compatibility pointers so older automation does not silently receive stale architecture. Existing contracts, version commands, Make targets and CI are preserved and extended. No product route, API response or user data behavior changes in 3.2.0.
