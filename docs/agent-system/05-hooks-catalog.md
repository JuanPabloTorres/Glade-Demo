# Hooks catalog

SessionStart reports branch/version/task. PreToolUse blocks destructive commands, writes on main, missing task ownership and unauthorized shared-file edits. PostToolUse recommends targeted checks. Stop/TaskCompleted requires a change fragment and completion metadata. Worktree hooks maintain a registry. Git hooks enforce architecture/contracts/i18n, Conventional Commits and the full pre-push gate.
