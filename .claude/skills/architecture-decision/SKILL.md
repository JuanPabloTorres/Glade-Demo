---
name: architecture-decision
description: Record a durable ADR before adopting a dependency, persistence/auth strategy, incompatible contract or transversal abstraction.
---
# Architecture decision

Create `docs/decisions/NNNN-<slug>.md` from the ADR template. Include context, forces, considered options, decision, consequences, migration, rollback and validation. Distinguish current capability from target architecture. Update project memory only after the ADR is accepted.
