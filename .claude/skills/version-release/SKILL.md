---
name: version-release
description: Classify SemVer, consolidate change fragments, synchronize version files and update release evidence.
---
# Version and release

Choose patch/minor/major from actual public impact. Individual delivery may bump once at completion; parallel worktrees never bump. Integration-manager runs the repository version command, updates release notes, validates against `origin/main`, confirms displayed/API version and records limitations.
