-r ../requirements.txt

# Optional local/open-source AI runtime. Not installed in the lightweight Vercel function.
torch>=2.8,<3.0
transformers>=4.55,<5.0
sentence-transformers>=5.0,<6.0
accelerate>=1.10,<2.0

# Document ingestion (master instruction §7.5).
docling>=2.60,<3.0
pymupdf>=1.24,<2.0
python-docx>=1.1,<2.0
openpyxl>=3.1,<4.0

# Local RAG (master instruction §7.6). FAISS is the default; chromadb is an
# alternative backend behind the same CaseDocumentIndex interface.
faiss-cpu>=1.9,<2.0
chromadb>=0.5,<1.0
