fastapi>=0.116,<1.0
pydantic>=2.11,<3.0
pydantic-settings>=2.10,<3.0
PyJWT>=2.10,<3.0
pwdlib[argon2]>=0.3,<1.0
pandas>=3.0,<4.0
rapidfuzz>=3.14,<4.0
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
# Strands orchestration against OpenAI (ADR 0002). Present here, unlike the
# ML stack, because it is 61 MB on a 250 MB budget and it is the only way this
# deployment target can run the agent at all: Ollama needs a local server, and
# a serverless function has none. See docs/DEPLOYMENT.md.
strands-agents[openai]>=1.50,<2.0
# Postgres driver. Present even though the default DSN is SQLite: Vercel's
# filesystem is per-instance and ephemeral, so a demo that needs its data to
# survive a cold start has to point DATABASE_URL at managed Postgres — and
# shipping the driver means that is a one-variable change rather than a code
# change plus a redeploy. ~4 MB.
psycopg[binary]>=3.2,<4.0
