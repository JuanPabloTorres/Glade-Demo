{
  "name": "matter-ready-demo",
  "private": true,
  "version": "3.1.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "install:web": "npm --prefix frontend ci --no-audit --no-fund",
    "build": "npm --prefix frontend run build",
    "version:check": "node scripts/versioning.mjs check",
    "version:check-bump": "node scripts/versioning.mjs check-bump",
    "version:check-against": "node scripts/versioning.mjs check-against",
    "version:current": "node scripts/versioning.mjs current",
    "version:major": "node scripts/versioning.mjs major",
    "version:minor": "node scripts/versioning.mjs minor",
    "version:patch": "node scripts/versioning.mjs patch"
  }
}
